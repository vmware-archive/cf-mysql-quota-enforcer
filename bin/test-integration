#!/bin/bash

set -eux

MY_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}"  )" && pwd  )"

# This script assumes that it lives one directory below the src directory.
SRC_DIR="$( cd $MY_DIR/.. && pwd  )"

ginkgo -r "$@" ${SRC_DIR}/integration
